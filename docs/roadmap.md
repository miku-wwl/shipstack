# 路线图

## 已实现

- `targets/ecs/`：以本地优先为原则的 ECS 应用 CI/CD，使用 Terraform 以及原生
  CodePipeline、CodeBuild、ECR、ECS、ALB 和 CloudWatch Logs。

## 未来的并列目标

- `targets/eks/`
- `targets/lambda/`
- `targets/apprunner/`

这些目前仅属于路线图项目。本次任务不会为它们创建空目录或占位实现。
